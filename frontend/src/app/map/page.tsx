// OWNED BY: frontend offline+map agent. Replace with the Leaflet hotspot map
// (zone polygons, CCTV/coverage layers, separation heat, kiosk recommendations).
export default function MapPage() {
  return (
    <div className="rounded-2xl border border-dashed border-border bg-card p-10 text-center text-muted">
      Hotspot map — coming up (zones, coverage, separation heat, kiosk gaps).
    </div>
  );
}
