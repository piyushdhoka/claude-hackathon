// OWNED BY: frontend wizard agent. Replace with the Search & Match Review screen
// (ranked candidates, per-feature explanation, Claude rationale, confirm flow).
export default function ReviewPage() {
  return (
    <div className="rounded-2xl border border-dashed border-border bg-card p-10 text-center text-muted">
      Search & Match review — coming up (ranked cross-center candidates + “why”).
    </div>
  );
}
