// OWNED BY: frontend wizard agent. Replace this stub with the tap-only,
// multilingual, voice-guided intake wizard (lost + found modes).
export default function IntakePage() {
  return (
    <div className="rounded-2xl border border-dashed border-border bg-card p-10 text-center text-muted">
      Intake wizard — coming up (tap-only, multilingual, voice-guided).
    </div>
  );
}
