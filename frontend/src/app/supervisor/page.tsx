// OWNED BY: frontend wizard agent (supervisor views). Replace with confirm-match,
// reveal-contact (PII), duplicate review, and audit-trail screens.
export default function SupervisorPage() {
  return (
    <div className="rounded-2xl border border-dashed border-border bg-card p-10 text-center text-muted">
      Supervisor console — coming up (confirm matches, reveal contact, audit).
    </div>
  );
}
